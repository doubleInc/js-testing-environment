{
  "name": "js-testing-environment",
  "version": "1.0.0",
  "description": "",
  "scripts": {
    "start": "concurrently \"lite-server -c ./bs-config.json\" \"npm run watch\"",
    "server": "lite-server -c ./bs-config.json",
    "test": "jest --config ./jest.config.json",
    "watch": "nodemon --ext js --watch ./__tests__ --exec \"npm run test\""
  },
  "author": "douleinc",
  "license": "ISC",
  "devDependencies": {
    "@babel/core": "^7.10.4",
    "@babel/preset-env": "^7.10.4",
    "babel-jest": "^26.1.0",
    "jest": "^26.1.0",
    "jest-html-reporters": "^2.0.1",
    "lite-server": "^2.5.4",
    "nodemon": "^2.0.4",
    "concurrently": "^5.2.0"
  }
}
